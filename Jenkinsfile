pipeline{
    agent any
    stages{
        stage('Submit stack'){
            steps{
                sh "aws cloudfromation create-stack --stack-name s3bucket --templatebody file://
            }
        }
}
